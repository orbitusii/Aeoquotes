# Aeoquotes: A Hyper-specialized Discord Quoting Bot

## Building/Running
Aeoquotes uses .NET 10 with no native dependencies, so just run `dotnet run` in the repository root to start the bot. Ensure that the `token.txt` file is in the repository root and that the SQLite database is located at `<root>/Database/quotes.db`.